/* Tests will be provided */
/* global describe, it */

// use 'jasmine JASMINE_CONFIG_PATH=[pass to json file]/jasmine.json' to run tests

// import assert from "assert"; //this string doesn't work

describe("# Null test", () => {
  it("check for nothing", () => {
    expect(true).toBe(true); //     assert(true); //so that this string doesn't work as well
  });
});
